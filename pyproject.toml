[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F"]
ignore = [
    "E501",    # line too long
    "BLE001",  # blind exception
    "UP045",   # Optional vs | None
]
